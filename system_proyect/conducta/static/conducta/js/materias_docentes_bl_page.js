/* materias_docentes_bl_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #materias_docentes_bl-data. */
window._MATERIAS = JSON.parse(document.getElementById('materias_docentes_bl-data').textContent);


