/* inventario_por_categoria_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #inventario_por_categoria-data. */
window._CAT_PAGE = JSON.parse(document.getElementById('inventario_por_categoria-data').textContent);


