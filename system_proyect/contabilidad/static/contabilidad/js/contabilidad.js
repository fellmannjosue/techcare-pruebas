/* <--- hecho por claude code: JS base del módulo Contabilidad (FASE 1, placeholder).
   La lógica de cada submódulo se agrega en su fase, en su propio archivo si crece. */
(function () {
  'use strict';
})();
