/* reporte_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #reporte-data. */
window._PAGE = JSON.parse(document.getElementById('reporte-data').textContent);


