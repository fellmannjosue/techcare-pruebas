/* sabado_form_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #sabado_form-data. */
window._PAGE = JSON.parse(document.getElementById('sabado_form-data').textContent);


