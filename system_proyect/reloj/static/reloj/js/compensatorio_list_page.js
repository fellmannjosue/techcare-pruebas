/* compensatorio_list_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #compensatorio_list-data. */
window._PAGE = JSON.parse(document.getElementById('compensatorio_list-data').textContent);


