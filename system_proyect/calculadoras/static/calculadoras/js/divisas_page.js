/* divisas_page.js — <--- hecho por claude code: extraído del template.
   Los datos de Django llegan por la isla JSON #divisas-data. */
window._PAGE = JSON.parse(document.getElementById('divisas-data').textContent);


